import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  height: 200px;
  align-items: center;
  background-color: #122ab2;

  p {
      flex: 1;
      text-align: center;
      font-size: 2.25rem;
      color: var(--white);
  }
`;
