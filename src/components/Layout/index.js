import React, { Fragment } from 'react'
import TopBar from "../../components/Layout";

const Layout = () => {
    return (
        <Fragment>
            <TopBar />
        </Fragment>
    )
}

export default Layout
